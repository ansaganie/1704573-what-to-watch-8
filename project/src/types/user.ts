export type User = {
  avatarUrl: string,
  email: string,
  id: number,
  name: string,
  token: string,
};

export type ServerUser = {
  'avatar_url': string,
  email: string,
  id: number,
  name: string,
  token: string,
}
